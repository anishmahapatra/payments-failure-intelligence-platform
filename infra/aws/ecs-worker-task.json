{
  "family": "payments-failure-intelligence-worker",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "512",
  "memory": "1024",
  "containerDefinitions": [
    {
      "name": "worker",
      "image": "<account>.dkr.ecr.<region>.amazonaws.com/payments-failure-intelligence-worker:latest",
      "essential": true,
      "command": ["sh", "-c", "python scripts/init_db.py && python -m app.workers.runner"],
      "environment": [
        { "name": "APP_ENV", "value": "production" },
        { "name": "DATABASE_URL", "value": "<rds-connection-string>" },
        { "name": "REDIS_URL", "value": "<elasticache-redis-url>" },
        { "name": "REDIS_QUEUE_NAME", "value": "payment-batch-jobs" },
        { "name": "MLFLOW_TRACKING_URI", "value": "<optional-mlflow-url>" },
        { "name": "MODEL_ARTIFACT_PATH", "value": "/app/artifacts/model_bundle.joblib" }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/payments-failure-intelligence/worker",
          "awslogs-region": "<region>",
          "awslogs-stream-prefix": "ecs"
        }
      }
    }
  ]
}
